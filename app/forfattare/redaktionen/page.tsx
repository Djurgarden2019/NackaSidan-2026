import Link from 'next/link';
import { articles } from '../../../content/articles';

export const metadata={title:'Redaktionen',description:'Möt NackaSidans redaktionella arbetssätt, bevakningsområden och senaste publiceringar.'};

const principles=[['Källor','Centrala faktauppgifter ska kunna följas tillbaka till en tydlig källa.'],['Analys','Tolkning och slutsatser ska vara begripliga och skiljas från verifierade fakta.'],['Rättelser','Fel ska rättas öppet och så snabbt som möjligt.'],['Relevans','Vi prioriterar det som hjälper läsaren förstå vad som förändras och varför.']];

export default function AuthorPage(){return <main><div className="shell"><section className="author-hero"><div className="author-monogram">NS</div><div><div className="kicker">Om oss</div><h1>NackaSidans redaktion</h1><p>Vi skriver om Stockholm, Sverige och världen med fokus på sammanhang, tydliga källor och praktisk betydelse.</p><div className="expertise"><span>Politik</span><span>Ekonomi</span><span>Geopolitik</span><span>AI</span><span>Kultur</span><span>Sport</span></div></div></section><section className="section"><div className="kicker">Så arbetar vi</div><h2>Fyra redaktionella löften</h2><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:14,marginTop:18}}>{principles.map(([title,text],index)=><article key={title} style={{borderTop:'3px solid #111',paddingTop:12}}><div className="meta">0{index+1}</div><h3>{title}</h3><p>{text}</p></article>)}</div><p style={{marginTop:22}}><Link className="text-link" href="/redaktionella-principer">Läs våra redaktionella principer →</Link></p></section><section className="section"><div className="section-heading"><h2>Senaste artiklarna</h2><p>{articles.length} publicerade texter i det centrala artikelregistret.</p></div><div className="author-articles">{articles.map(article=><article key={article.slug}><div className="kicker">{article.section}</div><h2><Link href={`/artikel/${article.slug}`}>{article.title}</Link></h2><p>{article.intro}</p><div className="meta">{article.updated} · {article.readingTime} läsning</div></article>)}</div></section></div></main>}
