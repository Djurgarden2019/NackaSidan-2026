import {AnalysisBox} from '@/components/Cards';
import {swedenStories} from '@/content/data';
export default function Sverige(){return <main><div className="shell"><header className="page-hero"><div className="kicker">Aktuellt · Sverige</div><h1>Sverige</h1><p>Veckans viktigaste svenska nyheter – med bakgrund, konsekvenser och redaktionell analys.</p></header><div className="article-list">{swedenStories.map((s,i)=><article className="article-row" key={s.title}><div className="meta">0{i+1} · 6 min</div><div><div className="kicker">{s.section}</div><h2>{s.title}</h2><p>{s.summary} Utvecklingen går inte att förstå isolerat. Den hänger samman med långsiktiga förändringar i ekonomi, teknik och medborgarnas förtroende för offentliga institutioner.</p><AnalysisBox>På kort sikt avgörs mycket av hur tydligt ansvariga aktörer kommunicerar. På längre sikt handlar frågan om Sveriges förmåga att anpassa institutioner och arbetsmarknad till snabb teknisk och ekonomisk förändring.</AnalysisBox></div></article>)}</div></div></main>}
