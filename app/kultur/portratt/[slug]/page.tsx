import type {Metadata} from 'next';import Link from 'next/link';import {notFound} from 'next/navigation';import {cultureProfiles,cultureProfileBySlug} from '../../../../content/cultureDesk';
export function generateStaticParams(){return cultureProfiles.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const profile=cultureProfileBySlug[slug];return profile?{title:profile.title,description:profile.intro}:{title:'Porträtt saknas'}}
export default async function ProfilePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const profile=cultureProfileBySlug[slug];if(!profile)notFound();return <main><div className="shell"><article className="article article-premium culture-profile"><nav className="meta"><Link href="/">NackaSidan</Link> · <Link href="/kultur">Kultur</Link> · {profile.role}</nav><div className="article-part-label">01 · Rubrik</div><div className="kicker">Idéer & människor · {profile.role}</div><h1>{profile.title}</h1><p className="intro">{profile.intro}</p><p className="meta">Publicerad 28 augusti 2026 · NackaSidans kulturredaktion</p><div className="article-part-label">02 · Själva artikeln</div><section className="culture-profile-facts"><div className="kicker">Tre centrala idéer</div><ul>{profile.ideas.map(idea=><li key={idea}>{idea}</li>)}</ul></section><div className="article-part-label">03 · Analys och konsekvenser</div><div className="article-body">{profile.sections.map(section=><section className="article-section" key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map(paragraph=><p key={paragraph}>{paragraph}</p>)}</section>)}</div><div className="article-part-label">04 · Källhänvisning</div><section className="sport-article-sources"><h2>Klickbara källor</h2><ul>{profile.sources.map(source=><li key={source.url}><a href={source.url} target="_blank" rel="noopener noreferrer">{source.label} ↗</a></li>)}</ul></section><div style={{display:'flex',gap:15,flexWrap:'wrap',marginTop:30}}><Link className="button" href="/kultur">Till Kultur</Link><Link className="text-link" href="/kulturdebatt">Kulturdebatt →</Link></div></article></div></main>}
