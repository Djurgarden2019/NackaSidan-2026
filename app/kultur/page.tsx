import Link from 'next/link';
import {cultureStories} from '@/content/data';
import {AnalysisBox} from '@/components/Editorial';
export default function Culture(){return <main><div className="shell"><div className="page-hero"><div className="kicker">Kultur</div><h1>Berättelserna som sätter tempot i perspektiv</h1><p>Böcker, film, TV-serier, musik och kulturdebatt – med recensioner, sammanhang och redaktionella analyser.</p></div><div className="article-list">{cultureStories.map((s,i)=><article className="article-row" key={s.title}><div className="meta">{String(i+1).padStart(2,'0')} · {s.section}</div><div><h2>{s.title}</h2><p>{s.summary}</p><AnalysisBox>{s.section==='Kulturdebatt'?'Kulturfrågan handlar sällan bara om smak. Den rör också makt, finansiering och vilka röster som får en varaktig plats i offentligheten.':'Det intressanta är inte bara verkets innehåll, utan vad publikens mottagande säger om vår tid och våra behov.'}</AnalysisBox>{s.href&&<Link className="text-link" href={s.href}>Läs vidare</Link>}</div></article>)}</div></main>}
