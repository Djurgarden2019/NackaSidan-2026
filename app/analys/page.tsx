import type {Metadata} from 'next';import Link from 'next/link';import {analyses} from '../../content/analyses';
export const metadata:Metadata={title:'Analys',description:'Fördjupande analyser av Sverige, världen, ekonomi, kultur, vetenskap, sport och Stockholm.'};
export default function AnalysisPage(){const [lead,...rest]=analyses;return <main><div className="shell"><section className="section"><div className="kicker">NackaSidan Analys · Uppdaterad 29 augusti 2026</div><h1 style={{fontFamily:'Georgia,serif',fontSize:'clamp(42px,7vw,82px)',lineHeight:.95,margin:'12px 0 18px'}}>Förstå krafterna bakom nyheterna</h1><p className="lead" style={{maxWidth:780}}>Längre resonemang med tydlig huvudtes, belagda fakta, osäkerheter och det redaktionen följer härnäst.</p></section><section className="hero" style={{borderTop:'5px solid #a61919'}}><div><div className="kicker">{lead.section} · Huvudanalys</div><Link href={`/analys/${lead.slug}`}><h2 style={{fontFamily:'Georgia,serif',fontSize:'clamp(34px,5vw,58px)',lineHeight:1}}>{lead.title}</h2></Link><p className="lead">{lead.dek}</p><p><strong>Huvudtes:</strong> {lead.thesis}</p><Link className="button" href={`/analys/${lead.slug}`}>Läs analysen</Link></div><div className="facts-panel"><div className="kicker">Analysen bygger på</div><ul>{lead.facts.map(fact=><li key={fact}>{fact}</li>)}</ul></div></section><section className="section"><div className="kicker">Fler analyser</div><h2>Åtta perspektiv på veckans avgörande frågor</h2><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(280px,1fr))',gap:22}}>{rest.map((item,index)=><article key={item.slug} style={{borderTop:index<2?'4px solid #a61919':'2px solid #111',paddingTop:14}}><div className="kicker">{item.section}</div><h3 style={{fontFamily:'Georgia,serif',fontSize:28,lineHeight:1.08,margin:'8px 0'}}><Link href={`/analys/${item.slug}`}>{item.title}</Link></h3><p>{item.dek}</p><p style={{color:'#555'}}><strong>Huvudtes:</strong> {item.thesis}</p><Link className="text-link" href={`/analys/${item.slug}`}>Läs hela analysen →</Link></article>)}</div></section></div></main>}
