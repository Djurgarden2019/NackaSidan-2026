import type {Metadata} from 'next';import Link from 'next/link';import {notFound} from 'next/navigation';import {sportArticles,sportArticleBySlug} from '../../../../content/sportArticles';
export function generateStaticParams(){return sportArticles.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const article=sportArticleBySlug[slug];return article?{title:article.title,description:article.dek}:{title:'Sportartikel saknas'}}
export default async function SportArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const article=sportArticleBySlug[slug];if(!article)notFound();const related=sportArticles.filter(item=>item.slug!==article.slug).slice(0,3);return <main><div className="shell"><article className="article article-premium sport-article"><nav className="meta" aria-label="Brödsmulor"><Link href="/">NackaSidan</Link> · <Link href="/sport">Sport</Link> · Artikel</nav><div className="kicker">{article.sport}</div><h1>{article.title}</h1><p className="intro">{article.dek}</p><p className="meta">Publicerad {article.date} · NackaSidans sportredaktion</p><section className="facts-panel"><div className="kicker">Verifierade fakta</div><h2>Detta vet vi</h2><ul>{article.facts.map(fact=><li key={fact}>{fact}</li>)}</ul></section><div className="article-body">{article.body.map(section=><section className="article-section" key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map(paragraph=><p key={paragraph}>{paragraph}</p>)}</section>)}</div><section className="editorial-analysis"><div className="kicker">Redaktionens analys</div><p>{article.analysis}</p></section><section className="consequence-panel"><div className="kicker">Detta följer vi</div><ul>{article.watch.map(point=><li key={point}>{point}</li>)}</ul></section><section className="sport-article-sources"><div className="kicker">Källor</div><ul>{article.sources.map(source=><li key={source.url}><a href={source.url} target="_blank" rel="noreferrer">{source.label} ↗</a></li>)}</ul></section><section className="section"><div className="kicker">Fler sportartiklar</div><div className="grid-3">{related.map(item=><article key={item.slug}><div className="kicker">{item.sport}</div><h3><Link href={`/sport/artikel/${item.slug}`}>{item.title}</Link></h3></article>)}</div></section><Link className="button" href="/sport">Till Sport</Link></article></div></main>}
