import type {Metadata} from 'next';import Link from 'next/link';import {notFound} from 'next/navigation';import {internationalFeatures} from '../../../content/internationalFeatures';
type Props={params:Promise<{slug:string}>};const bySlug=Object.fromEntries(internationalFeatures.map(a=>[a.slug,a]));
export function generateStaticParams(){return internationalFeatures.map(({slug})=>({slug}))}
export async function generateMetadata({params}:Props):Promise<Metadata>{const a=bySlug[(await params).slug];return a?{title:a.title,description:a.intro}:{}}
export default async function Page({params}:Props){const a=bySlug[(await params).slug];if(!a)notFound();return <main><article className="article article-premium"><nav className="meta"><Link href="/">NackaSidan</Link> · <Link href="/internationella-medier">Internationella medier</Link> · {a.section}</nav><div className="kicker">{a.section} · Egen svensk artikel</div><h1>{a.title}</h1><p className="intro">{a.intro}</p><p className="meta">Publicerad {a.published} · NackaSidans redaktion</p>{a.body.map(s=><section className="article-section" key={s.heading}><h2>{s.heading}</h2>{s.paragraphs.map(p=><p key={p}>{p}</p>)}</section>)}<section className="sport-article-sources"><h2>Källor</h2><ul>{a.sources.map(s=><li key={s.url}><a href={s.url} target="_blank" rel="noopener noreferrer">{s.label} ↗</a></li>)}</ul></section><Link className="button" href="/internationella-medier">Till Internationella medier</Link></article></main>}
