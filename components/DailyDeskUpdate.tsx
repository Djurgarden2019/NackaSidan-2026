import {dailyDeskUpdates,type DailyDeskKey} from '../content/dailyDeskUpdates';
export default function DailyDeskUpdate({desk}:{desk:DailyDeskKey}){const stories=dailyDeskUpdates[desk];return <section className="section daily-desk-update" aria-labelledby={`daily-${desk}`} style={{borderTop:'4px solid #a61919',marginTop:28,paddingTop:22}}><div className="kicker">Aktuellt · Uppdaterad 2 september 2026</div><h2 id={`daily-${desk}`} style={{fontFamily:'Georgia,serif',fontSize:'clamp(30px,4vw,46px)',margin:'8px 0 22px'}}>Dagens uppdatering</h2><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(280px,1fr))',gap:24}}>{stories.map(story=><article key={story.title} style={{borderTop:'1px solid #222',paddingTop:16}}><div className="kicker">{story.section}</div><h3 style={{fontFamily:'Georgia,serif',fontSize:26,lineHeight:1.1}}>{story.title}</h3><p><strong>Själva nyheten.</strong> {story.news}</p><p><strong>Analys och konsekvenser.</strong> {story.analysis}</p><p><strong>Längre fördjupning.</strong> {story.depth}</p><div><strong>Källor.</strong>{story.sources.map(source=><div key={source.url}><a href={source.url} target="_blank" rel="noopener noreferrer">{source.label} ↗</a></div>)}</div></article>)}</div></section>}