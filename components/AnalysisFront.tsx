import Link from 'next/link';import {analyses} from '../content/analyses';
export default function AnalysisFront(){const [lead,...rest]=analyses;return <section className="section" aria-labelledby="analysis-front-title" style={{borderTop:'6px solid #111',paddingTop:22}}><div className="kicker">NackaSidan Analys</div><div style={{display:'flex',justifyContent:'space-between',gap:18,alignItems:'baseline',flexWrap:'wrap'}}><h2 id="analysis-front-title" style={{fontSize:'clamp(34px,5vw,54px)',margin:'8px 0'}}>Bakom rubrikerna</h2><Link className="text-link" href="/analys">Alla analyser →</Link></div><div className="analysis-front-grid"><article style={{borderTop:'4px solid #a61919',paddingTop:14}}><div className="kicker">{lead.section} · Huvudanalys</div><h3 style={{fontFamily:'Georgia,serif',fontSize:'clamp(30px,4vw,44px)',lineHeight:1.02,margin:'10px 0'}}><Link href={`/analys/${lead.slug}`}>{lead.title}</Link></h3><p className="lead">{lead.dek}</p><p><strong>Huvudtes:</strong> {lead.thesis}</p><Link className="button" href={`/analys/${lead.slug}`}>Läs hela analysen</Link></article><div>{rest.slice(0,3).map(item=><article key={item.slug} style={{borderTop:'1px solid #aaa',padding:'14px 0'}}><div className="kicker">{item.section}</div><h3 style={{fontFamily:'Georgia,serif',fontSize:23,lineHeight:1.08,margin:'6px 0'}}><Link href={`/analys/${item.slug}`}>{item.title}</Link></h3><p style={{margin:'6px 0'}}>{item.dek}</p></article>)}</div></div></section>}
